CREATE TABLE GAME(
   ID INT PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL
);

CREATE TABLE TOWER(
   ID INT PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL
);

CREATE TABLE DISK(
   ID INT PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL
);